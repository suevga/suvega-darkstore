// Deprecated in favor of useBackend. Kept for backwards compatibility and gradual migration.
export { useBackend as useApi } from './useBackend';
export type { UseBackendReturn as UseApiReturn } from './useBackend';
